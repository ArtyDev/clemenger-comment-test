{% extends '::base.html.twig' %}
{% block body %}
    {# list comments #}
    {% for comment in comments %}
        {{ include('ClemengerCommentBundle:Comment:single.html.twig', { 'comment': comment }) }}
    {% endfor %}
    {# display form #}
    {{ form(form) }}
{% endblock %}
