{% extends 'base.html.twig' %}
{% block body %}
<div class="comment">
    <img src="{{ gravatar(comment.email) }}" />
    {{ comment.name }}
    {{ comment.date | date}}
    {{ comment.comment }}
</div>
{% endblock %}
